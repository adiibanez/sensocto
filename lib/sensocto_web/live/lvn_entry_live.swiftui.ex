defmodule SensoctoWeb.Live.LvnEntryLive.SwiftUI do
  alias LiveViewNative.LVN
  use SensoctoNative, [:render_component, format: :swiftui]
  require Logger

  # def render(assigns, interface) do
  #   Logger.info("render #{inspect(interface)}")

  #   ~LVN"""
  #   <Text>Hello #{inspect(interface)}</Text>
  #   """
  # end
end
