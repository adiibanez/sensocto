defmodule SensoctoWeb.Live.LvnSigninLive.SwiftUI do
  # alias LiveViewNative.LVN
  use SensoctoNative, [:render_component, format: :swiftui]
  require Logger

  def _render(assigns, interface) do
    ~LVN"""
    <Text>Hello SwiftUI</Text>
    """
  end
end
