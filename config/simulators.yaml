# Simulator configuration
# This is the default/base config file loaded at startup.
# Use scenario files in config/simulator_scenarios/ for different simulation setups.
#
# To use a scenario, go to /simulator and select one from the list.
# Scenarios are loaded from config/simulator_scenarios/*.yaml

connectors: {}
