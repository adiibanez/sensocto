defmodule Sensocto.Repo.Migrations.RenameIntimateTrustedToSensualocto do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users) do
      remove :intimate_trusted
      add :sensualocto, :boolean, null: false, default: false
    end
  end

  def down do
    alter table(:users) do
      remove :sensualocto
      add :intimate_trusted, :boolean, null: false, default: false
    end
  end
end
