defmodule SensoctoWeb.IndexLive.SwiftUI do
  use SensoctoNative, [:render_component, format: :swiftui]

  # use SensoctoNative, [:render_component, format: :swiftui]
  # use SensoctoNative, [:live_view, format: :swiftui]
  # use SensoctoNative, [:component, format: :swiftui]
end
